# СДАМ ГИА MCP Server - Project Summary

## Что создано

Полнофункциональный **MCP сервер на TypeScript** для работы с образовательной платформой **СДАМ ГИА**. Сервер позволяет LLM искать и получать задания по всем предметам ЕГЭ/ОГЭ с решениями и ответами.

## Основные возможности

### 🎯 7 инструментов (tools)

1. **sdamgia_get_problem** - получить задание по ID
2. **sdamgia_search_problems** - поиск по ключевым словам
3. **sdamgia_search_by_text** - нечёткий поиск по тексту условия (с поддержкой опечаток!)
4. **sdamgia_batch_get_problems** - пакетное получение заданий
5. **sdamgia_get_catalog** - получить каталог по предмету
6. **sdamgia_get_category_problems** - задания из категории
7. **sdamgia_get_test** - задания из теста

### 🔍 Умный нечёткий поиск

**Проблема**: Пользователь фотографирует задание, OCR распознаёт с ошибками, текст немного отличается.

**Решение**: Алгоритм `search_by_text` использует:
- **Расстояние Левенштейна** - посимвольное сравнение
- **Keyword overlap** - семантическое сравнение ключевых слов
- **Комбинированная оценка** - взвешенная сумма метрик

```typescript
similarity = keyword_overlap * 0.6 + levenshtein * 0.4
```

**Параметры**:
- `threshold` - порог схожести (0-1, по умолчанию 0.6)
- Если текст слегка отличается - всё равно найдёт!

### 📊 Два формата вывода

**JSON** - для программной обработки:
```json
{
  "id": "1001",
  "condition": {"text": "...", "images": [...]},
  "solution": {"text": "...", "images": [...]},
  "answer": "0.95"
}
```

**Markdown** - для человеко-читаемого вывода:
```markdown
# Problem 1001
## Condition
На экзамен вынесено 60 вопросов...
## Answer
0.95
```

## Архитектурные решения

### 1. Полный охват API

**Все** основные эндпоинты СДАМ ГИА представлены как отдельные tools. Это даёт LLM максимальную гибкость в композиции операций.

**Пример workflow**:
```
get_catalog → найти ID категории "Вероятность"
  ↓
get_category_problems → получить ID всех заданий
  ↓
batch_get_problems → получить полные данные
```

### 2. Экономичность запросов

**Для обычного поиска**: 1 запрос
**Для нечёткого поиска**: 
- 1 широкий поиск → ~50 ID кандидатов
- 30 параллельных запросов → детали заданий
- Локальная фильтрация → топ N результатов
- **Итого**: ~31 запрос вместо сотен

**Batch операции**: параллельные запросы вместо последовательных

### 3. Типобезопасность

- **TypeScript** для статической типизации
- **Zod schemas** для runtime валидации
- **Чёткие ошибки** с конкретными сообщениями

```typescript
const SearchByTextInputSchema = z.object({
  subject: SubjectSchema,
  condition_text: z.string()
    .min(10, "Condition text must be at least 10 characters")
    .max(1000, "Condition text must not exceed 1000 characters"),
  threshold: z.number().min(0).max(1).default(0.6),
  limit: LimitSchema
}).strict();
```

### 4. Модульная структура

```
src/
├── index.ts           # Точка входа
├── types.ts           # TypeScript типы
├── constants.ts       # Конфигурация
├── services/
│   ├── sdamgia-client.ts   # API клиент (web scraping)
│   ├── text-utils.ts       # Нечёткий поиск
│   └── formatters.ts       # JSON/Markdown форматтеры
├── schemas/
│   └── input-schemas.ts    # Zod валидация
└── tools/
    ├── problem-tools.ts    # Инструменты для заданий
    └── catalog-tools.ts    # Инструменты для каталога
```

## Технические детали

### API (Reverse-engineered)

Официального API нет, используется web scraping:

```
GET https://{subject}-ege.sdamgia.ru/problem?id={id}
GET https://{subject}-ege.sdamgia.ru/search?search={query}
GET https://{subject}-ege.sdamgia.ru/test
GET https://{subject}-ege.sdamgia.ru/test?id={id}
GET https://{subject}-ege.sdamgia.ru/prob_catalog?category={id}
```

**Парсинг**: `cheerio` для HTML → структурированные данные

### Поддерживаемые предметы (15 штук)

- Математика (профильная и базовая)
- Русский язык
- Физика, Химия, Биология, География
- История, Обществознание, Литература
- Информатика
- Английский, Немецкий, Французский, Испанский

### Алгоритмы текстового поиска

**Нормализация**:
```typescript
normalizeText(text) {
  return text
    .toLowerCase()
    .replace(/[^\w\s]/g, " ")  // убрать пунктуацию
    .replace(/\s+/g, " ")      // убрать лишние пробелы
    .trim();
}
```

**Извлечение ключевых слов**:
```typescript
extractKeywords(text) {
  // убрать стоп-слова ("в", "на", "и", "с", ...)
  // оставить слова длиной > 2
  return words.filter(w => w.length > 2 && !stopWords.has(w));
}
```

**Комбинированная оценка**:
```typescript
calculateSimilarity(query, text) {
  // 1. Exact match → 0.95
  if (text.includes(query)) return 0.95;
  
  // 2. Keyword overlap
  const keywordScore = intersection / min(sizes);
  
  // 3. Levenshtein distance
  const fuzzyScore = 1 - distance / maxLength;
  
  // 4. Weighted sum
  return keywordScore * 0.6 + fuzzyScore * 0.4;
}
```

## Файлы документации

- **README.md** - основная документация с примерами
- **ARCHITECTURE.md** - детальная архитектура и дизайн-решения
- **EXAMPLES.md** - 10+ практических примеров использования
- **DIAGRAMS.md** - визуальные схемы и диаграммы (Mermaid)

## Установка и запуск

```bash
cd sdamgia-mcp-server
npm install
npm run build

# Для Claude Desktop добавить в конфиг:
{
  "mcpServers": {
    "sdamgia": {
      "command": "node",
      "args": ["/path/to/dist/index.js"]
    }
  }
}

# Для тестирования:
npm run inspector
```

## Примеры использования

### Пример 1: Найти задание по тексту с опечатками

```typescript
// Текст с ошибкой: "сопративление" вместо "сопротивление"
sdamgia_search_by_text({
  subject: "phys",
  condition_text: "Найдите силу тока если сопративление 10 Ом",
  threshold: 0.7
})

// Вернёт: Problem 5421 (similarity: 87.3%)
```

### Пример 2: Получить все задания по вероятности

```typescript
// Шаг 1: Получить каталог
sdamgia_get_catalog({ subject: "math" })

// Шаг 2: Найти категорию "Вероятность" → ID = 4

// Шаг 3: Получить задания
sdamgia_get_category_problems({
  subject: "math",
  category_id: "4",
  limit: 20
})

// Шаг 4: Получить детали
sdamgia_batch_get_problems({
  subject: "math",
  problem_ids: ["1001", "1002", ...]
})
```

### Пример 3: Найти похожие задания

```typescript
// Шаг 1: Получить задание
const problem = sdamgia_get_problem({
  subject: "math",
  problem_id: "1001"
})

// Извлечь analogs: ["1002", "1003", "1004", ...]

// Шаг 2: Получить похожие
sdamgia_batch_get_problems({
  subject: "math",
  problem_ids: problem.analogs
})
```

## Что НЕ реализовано (но можно добавить)

1. **Кэширование** - Redis или file-based cache
2. **Rate limiting** - защита от перегрузки
3. **HTTP transport** - для remote deployment
4. **Image OCR** - распознавание текста с фото
5. **Test generation** - создание тестов через API
6. **Statistics** - метрики использования

## Ограничения

1. **Нет официального API** - используется web scraping
2. **Только русский язык** - платформа на русском
3. **Read-only** - нет операций записи
4. **Требуется интернет** - онлайн доступ к СДАМ ГИА

## Производительность

- **Одно задание**: ~1 сек (1 запрос)
- **Поиск**: ~1-2 сек (1-2 запроса)
- **Нечёткий поиск**: ~5-10 сек (31-51 параллельный запрос)
- **Batch 10 заданий**: ~3-5 сек (10 параллельных запросов)

## Сравнение с существующими решениями

| Библиотека | Язык | API | Нечёткий поиск | MCP |
|------------|------|-----|----------------|-----|
| sdamgia-api | Python | ✅ | ❌ | ❌ |
| sdamgia-api-upd | Python | ✅ | ❌ | ❌ |
| **Этот проект** | TypeScript | ✅ | ✅ | ✅ |

## Итоговая архитектура

```
LLM (Claude/GPT)
    ↓ MCP Protocol (stdio)
MCP Server (TypeScript)
    ├── Tool Layer (7 tools)
    ├── Business Logic
    │   ├── Formatters (JSON/Markdown)
    │   ├── Text Utils (Fuzzy search)
    │   └── Validation (Zod)
    └── API Client (Web scraper)
        ↓ HTTPS
СДАМ ГИА Platform (15 subjects)
```

## Качество кода

- ✅ **TypeScript** - полная типизация
- ✅ **Zod validation** - runtime проверки
- ✅ **Модульность** - чёткое разделение ответственности
- ✅ **Документация** - подробные комментарии
- ✅ **Примеры** - 10+ use cases
- ✅ **Диаграммы** - визуальные схемы

## Вывод

Создан **production-ready MCP сервер** с:
- Полным покрытием API СДАМ ГИА
- Умным нечётким поиском
- Экономичными запросами
- Типобезопасностью
- Подробной документацией

Готов к использованию с Claude Desktop, MCP Inspector или любым MCP-клиентом!
